<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ztz.system.dao.UserMapper">

    <resultMap id="BaseResultMap" type="com.ztz.system.pojo.User">
        <id column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="user_type" jdbcType="INTEGER" property="userType"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_id, username, password, user_type
    </sql>

    <!--通过id查找单个用户-->
    <select id="getUserById" parameterType="java.lang.Integer" resultType="com.ztz.system.pojo.User">
        SELECT *
        FROM user
        WHERE user_id = #{id,jdbcType=INTEGER}
    </select>

    <!--通过username查询单个用户-->
    <!--User getUserByUsername(String username);-->
    <select id="getUserByUsername" parameterType="java.lang.String" resultType="com.ztz.system.pojo.User">
        SELECT *
        FROM user
        WHERE username = #{username, jdbcType=VARCHAR}
    </select>

    <!--查找所有用户-->
    <select id="getAllUsers" resultMap="BaseResultMap">
        select *
        FROM user
    </select>

    <!--// 改-->
    <!--int updateUser(User user);-->
    <update id="updateUser" parameterType="com.ztz.system.pojo.User">
        update user
        <set>
            <if test="username != null">
                username = #{username,jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                password = #{password,jdbcType=VARCHAR},
            </if>
            <if test="userType != null">
                user_type = #{userType,jdbcType=INTEGER},
            </if>
        </set>
        where user_id = #{userId,jdbcType=INTEGER}
    </update>

    <!--// 增-->
    <!--int insertUser(User user);-->
    <insert id="insertUser" parameterType="com.ztz.system.pojo.User">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">
                user_id,
            </if>
            <if test="username != null">
                username,
            </if>
            <if test="password != null">
                password,
            </if>
            <if test="userType != null">
                user_type,
            </if>
        </trim>

        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">
                #{userId,jdbcType=INTEGER},
            </if>
            <if test="username != null">
                #{username,jdbcType=VARCHAR},
            </if>
            <if test="password != null">
                #{password,jdbcType=VARCHAR},
            </if>
            <if test="userType != null">
                #{userType,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete from user
        where user_id = #{id,jdbcType=INTEGER}
    </delete>

    <!--<insert id="insertUser" parameterType="com.ztz.system.pojo.User">-->
    <!--insert into user (user_id, username, password, -->
    <!--type)-->
    <!--values (#{user_id,jdbcType=INTEGER}, #{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}, -->
    <!--#{type,jdbcType=INTEGER})-->
    <!--</insert>-->


    <!--<update id="updateByPrimaryKey" parameterType="com.ztz.system.pojo.User">-->
    <!--update user-->
    <!--set username = #{username,jdbcType=VARCHAR},-->
    <!--password = #{password,jdbcType=VARCHAR},-->
    <!--type = #{type,jdbcType=INTEGER}-->
    <!--where user_id = #{user_id,jdbcType=INTEGER}-->
    <!--</update>-->

</mapper>